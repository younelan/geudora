/* portable-compat.h — EMPTY. All Mac types replaced with standard C.
   This file is kept only because some headers still #include it.
   Safe to remove once all #include "portable-compat.h" are cleaned up. */
#ifndef PORTABLE_COMPAT_H
#define PORTABLE_COMPAT_H
#endif
